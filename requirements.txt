# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn==0.32.1
slowapi==0.1.9

# ── Data validation ────────────────────────────────────────────────────────
pydantic==2.10.3
python-dotenv==1.0.1

# ── LLM + agent framework ──────────────────────────────────────────────────
# LangGraph 0.3.x (breaking change from 0.2.x; pinned for reproducibility)
# LangChain 0.3.x is the compatible series for LangGraph 0.3
openai==1.59.3
langgraph==0.3.5
langchain==0.3.13
langchain-community==0.3.13
langchain-openai==0.3.3

# ── Database ───────────────────────────────────────────────────────────────
psycopg2-binary==2.9.10
pgvector==0.3.6
# Database migrations
alembic==1.14.0

# ── Web search (production provider) ──────────────────────────────────────
tavily-python==0.5.0

# ── Utilities ─────────────────────────────────────────────────────────────
pandas==2.2.3
requests==2.32.3
httpx==0.28.1

# ── Testing ────────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0

# ── Dev-only: DuckDuckGo fallback (unreliable in production — use Tavily) ──
# Remove this line if deploying to production without DuckDuckGo support.
duckduckgo-search==6.2.6
